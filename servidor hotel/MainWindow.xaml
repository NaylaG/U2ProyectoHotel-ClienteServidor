<Window x:Class="servidor_hotel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:servidor_hotel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DataGrid ItemsSource="{Binding Huespedes.Huespedes}" IsReadOnly="True" AutoGenerateColumns="False"  >
        <DataGrid.Columns>
            <DataGridTextColumn Header="Clave" Binding="{Binding ClaveReservacion}"></DataGridTextColumn>
            <DataGridTextColumn Header="Nombre"   Binding="{Binding Nombre}"></DataGridTextColumn>
            <DataGridTextColumn Header="Fecha de entrada"  Binding="{Binding FechaEntrada, StringFormat=\{0:d\}}"></DataGridTextColumn>
            <DataGridTextColumn Header="Fecha de salida"   Binding="{Binding FechaSalida, StringFormat=\{0:d\}}"></DataGridTextColumn>
            <DataGridTextColumn Header="Tipo de habitación"   Binding="{Binding TipoHabitacion}"></DataGridTextColumn>
            <DataGridTextColumn Header="Numer de personas"   Binding="{Binding NumPersonas}"></DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>

</Window>
